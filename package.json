{
  "name": "ubique-application-crm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "preflight": "node scripts/preflight.mjs",
    "security:scan": "node scripts/secret-scan.mjs",
    "verify": "npm run security:scan && npm run typecheck && npm run build",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "next": "^15.5.0",
    "openai": "^5.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": "22.x"
  }
}
