const prompt =
  "Connexion 200 caractères max, puis message après connexion 600 caractères max et InMail 1000 caractères max. Adapter contact réellement identifié, ne pas inventer prénom.";

export default prompt;
