const ok=(v?:string)=>v?'Configuré':'À configurer';
export default function Settings(){const rows=[['OpenAI',ok(process.env.OPENAI_API_KEY)],['Google OAuth',ok(process.env.GOOGLE_CLIENT_ID&&process.env.GOOGLE_CLIENT_SECRET)],['Google background refresh token',ok(process.env.GOOGLE_REFRESH_TOKEN)],['Tavily web research',ok(process.env.TAVILY_API_KEY)],['Cron secret',ok(process.env.CRON_SECRET)]];return <div className="page"><div className="page-head"><div><h1>Paramètres</h1><p>Le site reste utilisable en mode démo tant que les clés ne sont pas renseignées.</p></div><a className="button primary" href="/api/auth/google/start">Connecter Google</a></div><div className="panel"><div className="list">{rows.map(([k,v])=><div className="list-item" key={k}><strong>{k}</strong><span className="badge">{v}</span></div>)}</div></div><div className="notice" style={{marginTop:16}}>Aucun email ni candidature n’est envoyé automatiquement. Le produit prépare et classe ; l’envoi reste humain.</div></div>}
