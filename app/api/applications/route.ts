import { NextRequest, NextResponse } from 'next/server'; import crypto from 'crypto'; import { listApplications, saveApplication } from '@/lib/store'; import { Application } from '@/lib/types';
export async function GET(){return NextResponse.json(await listApplications())}
export async function POST(req:NextRequest){try{const body=await req.json();const now=new Date().toISOString();const app:Application={id:crypto.randomUUID(),company:body.company,jobTitle:body.jobTitle,officialUrl:body.officialUrl,jobSnapshot:body.jobSnapshot,location:body.location,vertical:body.vertical||[],priority:body.priority||'B',careerPriority:body.careerPriority||body.priority||'B',status:'Inbox',stage:'Candidature',nextAction:'Analyser et vérifier la source officielle',nextActionDate:now.slice(0,10),createdAt:now,updatedAt:now};await saveApplication(app);return NextResponse.json(app,{status:201})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
