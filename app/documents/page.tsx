import { listApplications } from '@/lib/store';
export default async function Documents(){const apps=await listApplications();return <div className="page"><div className="page-head"><div><h1>Documents</h1><p>Versioning des CV, lettres et livrables liés à chaque candidature.</p></div></div><div className="panel"><div className="list">{apps.filter(a=>a.coverLetterReady||a.cvVersion).map(a=><div className="list-item" key={a.id}><div><strong>{a.company} · {a.jobTitle}</strong><p>CV : {a.cvVersion||'non défini'} · LM : {a.coverLetterReady?'prête':'non'} · Email : {a.emailReady?'prêt':'non'}</p></div><span className="badge">{a.status}</span></div>)}</div></div></div>}
